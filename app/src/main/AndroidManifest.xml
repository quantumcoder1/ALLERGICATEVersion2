<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.allergicateversion2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <meta-data
        android:name="com.google.android.gms.games.APP_ID"
        android:value="@string/google_app_id" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ALLERGICATEVersion2"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ReviewsActivity"
            android:exported="false"
            android:label="@string/title_activity_reviews"
            android:theme="@style/Theme.ALLERGICATEVersion2" />
        <activity
            android:name=".RestaurantInformation"
            android:exported="false"
            android:theme="@style/Theme.ALLERGICATEVersion2"/> <!-- <activity -->
        <!-- android:name=".RestaurantDetailsScrollingActivity" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_scrolling" -->
        <!-- android:theme="@style/Theme.ALLERGICATEVersion2.NoActionBar" /> -->
        <activity
            android:name=".RestaurantInfo"
            android:exported="false" />
        <activity
            android:name=".SearchResultsMap"
            android:exported="false" />
        <activity
            android:name=".AllergyList"
            android:exported="false" />
        <activity
            android:name=".ForgotPassword"
            android:exported="false" />
        <activity
            android:name=".HomeScreen"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".ForgotPassword" -->
        <!-- android:exported="false" /> -->
        <activity android:name=".LoginActivity" /> <!-- android:exported="false" -->
        <!-- android:label="Login" /> -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY" />
    </application>

</manifest>
